# Example environment configuration for AI News Agent
# Copy this file to `.env` and fill in real values.

# --- LLM Provider Keys (Pick one provider) ---
# Gemini (Google) API Key (preferred if you have it)
GEMINI_API_KEY=your_gemini_api_key_here
# Optional override model (defaults to gemini-1.5-flash if unset)
GEMINI_MODEL=gemini-1.5-flash

# OR OpenAI API Key
# OPENAI_API_KEY=sk-...
# OPENAI_MODEL=gpt-4o-mini

# Allow placeholder articles when no real feed items found (true/false)
ALLOW_PLACEHOLDER_ARTICLES=true

# --- Email SMTP Settings ---
# Sender should generally be a Gmail address if using Gmail SMTP
SENDER_EMAIL=your_sender_email@example.com
# For Gmail: generate a 16-char App Password in Google Account > Security
SENDER_APP_PASSWORD=your_16_char_app_password_no_spaces
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587

# --- Recipients ---
# Primary daily recipient
RECIPIENT_EMAIL=recipient@example.com
# Optional test recipient fallback (used if RECIPIENT_EMAIL not set)
TEST_RECIPIENT_EMAIL=test_recipient@example.com

# --- Optional ---
# AGENT_MODEL=openai/gpt-4o-mini  # fallback symbolic model name if no provider keys


